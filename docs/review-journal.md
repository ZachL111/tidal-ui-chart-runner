# Review Journal

I treated `tidal-ui-chart-runner` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its frontend apps focus without claiming live deployment or external usage.

## Cases

- `baseline`: `view drift`, score 188, lane `ship`
- `stress`: `state pressure`, score 173, lane `ship`
- `edge`: `layout risk`, score 182, lane `ship`
- `recovery`: `interaction cost`, score 198, lane `ship`
- `stale`: `view drift`, score 232, lane `ship`

## Note

The repository should be understandable without pretending it is larger than it is.
